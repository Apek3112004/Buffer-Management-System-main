#ifndef _TYPES_HPP_
#define _TYPES_HPP_

#include <cstdint>
#include <cstddef>

typedef uint32_t frame_id_t;
typedef uint32_t page_id_t;

// Plain const for page size
const std::size_t PAGE_SIZE = 512;

#endif // _TYPES_HPP_
